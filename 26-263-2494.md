---
日付: 2026-09-20T16:16:24+09:00
とき: 2026-09-20T16:16:24+09:00
通り名: 見所探し
tags: []
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
型: "[[26-142-0390|ｸｴﾘ]]"
---

***
# あらまし
仕組み掛けが見るべき事柄を探し出す。

# 仕掛け
### 仕組み方
```JS
const [
	{arche, transDunamis,Melete:{getSections}},
	{Mneme:{tipos,messengers}},
] = await syzygy('26-263-2494');

const {time, fragment} = arche.format;
```
```JS
export default async function(noema){
	const kytos = messengers(tipos(this.current()));
	const dunamis = kytos.findLast(d=>d[time] < this.current()[time])
		?? kytos.shift();
	const khora = arche.format.fragment(eidolon[dunamis.file.name]);
	return await getSections(this.current())
		.then(x=>x.blocks[khora.replace('^','')].text)
		.then(t=>t.replace(khora,"").trim())
		.catch(console.error);
}
```
^gzb1