---
型: "[[25-201-3900|Modules]]"
とき: 2026-07-01 09:26
日付: 2026-07-01 09:26
tags:
  - inProgress
通り名: 付け方
カタ:
  - "[[DatumOvule.js]]"
  - "[[26-177-2179|事地]]"
  - "[[26-179-0856|言葉付け]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{
		arche,transDunamis, kataStasis,
		Melete: {getLinkText,assignTo}
	},
	exoDunamis, exoLogos,
] = await syzygy('26-182-5130');
const {title} = arche.prop;
```
```JS
export default async function (label,alias){
	if(alias && label!==alias)return exoLogos({hyle:label},alias);
	
	const dunamis = await exoDunamis({eidolon:true});
	const stasis = kataStasis
		(_=>transDunamis(true)(dunamis)?.[title])(dunamis);
	await assignTo(dunamis,title,label,"aliases");
	await stasis;
	const tropos = await getLinkText(dunamis)
		.then(x=> t=>t.replaceAll(`[[${label}]]`,x));
	const tropoKhora = Object
		.entries(app.metadataCache.unresolvedLinks)
		.filter(([_,x])=>Object.keys(x).includes(label))
		.map(([x,_])=>transDunamis()(x))
		.filter(Boolean);
	for(const khoroDunamis of tropoKhora)
		await app.vault.process(khoroDunamis,tropos);
	return dunamis;
}
```
^gzb1