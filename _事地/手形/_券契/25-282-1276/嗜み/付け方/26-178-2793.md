---
型: "[[25-201-3900|Modules]]"
とき: 2026-06-27 16:41
日付: 2026-06-27 16:41
tags:
  - inProgress
通り名: 時付け
カタ:
  - "[[DatumOvule.js]]"
  - "[[25-336-3648|方付け]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{arche, transDunamis, kataStasis, Mneme: {recentView}},
	endoCascade,
] = await syzygy('26-178-2793');
```
```JS
export default async function (hyle, morphe={}){
	const dunamis = transDunamis()(hyle) ?? recentView().file;
	if(dunamis?.extension !== "md")
		return await endoCascade(dunamis,morphe);
	const tropos = khora=> khora[arche.prop.time]=moment().format();
	const stasis = kataStasis(true)(dunamis);
	await app.fileManager.processFrontMatter(dunamis,tropos);
	await stasis.then(_=>new Notice('時を改めました'));
	
	return dunamis;
}
```
^gzb1