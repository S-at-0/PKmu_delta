---
型: "[[25-201-3900|Modules]]"
とき: 2026-06-26 20:04
日付: 2026-06-26 20:04
tags:
  - inProgress
通り名: 事地
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-171-5244|ｺﾄﾁﾞ]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{
		arche, transDunamis,
		Mneme:{cascade},
		Melete: {assignTo,getLinkText},
	},
	{
		Mneme: {eidolon},
		Melete: {setMorphen, mintStamp, isSanctum},
	},
] = await syzygy('26-177-2179');
```
```JS
export default async function (morphe={}){
	await setMorphen({
		hyle: mintStamp(),
		eidos: cascade(this).pop(),
		eidolon: "tab",
	},morphe);
	await qa.executeChoice('型付',morphe);
	
	const dunamis = transDunamis()(morphe.hyle);
	const khora = app.workspace.getLeavesOfType("markdown")  
        .map(x => x.view.file)
        .filter(isSanctum(true))
        .map(x=>getLinkText(x));
    const turrets = await Promise.all(khora);
	if(!isSanctum(true)(morphe.hyle) && turrets.length)
		await assignTo(dunamis,arche.prop.cascade, turrets);
	return transDunamis()(morphe.hyle);
}
```
^gzb1