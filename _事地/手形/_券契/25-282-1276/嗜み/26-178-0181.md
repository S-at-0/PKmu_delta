---
型: "[[25-201-3900|Modules]]"
とき: 2026-06-27 10:30
日付: 2026-06-27 10:30
tags:
  - inProgress
通り名: 手形
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-173-5157|仕立て方]]"
  - "[[26-177-2179|事地]]"
  - "[[26-171-3697|ﾃｶﾞﾀ]]"
  - "[[26-171-5046|型取り所]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{
		arche,transDunamis,kataStasis,
		Mneme:{cascade},
		Melete:{getLinkText},
	},
	{
		Mneme:{tipos,topos,messengers},
		Melete:{setMorphen,isMessenger,mintStamp},
	},
	khoroKinesis, exoDunamis,
] = await syzygy('26-178-0181');
```
```JS
export default async function (morphe={}){
	const [tally, ephemera] = cascade(this).slice(-2);
	await setMorphen({
		hyle: mintStamp(),
		eidos: app.workspace.getActiveFile(),
	},morphe);
	
	if(!isMessenger(morphe.eidos) && !messengers(morphe.eidos).length)
		return await exoDunamis({...morphe, eidos:ephemera})
	else await exoDunamis({...morphe, eidos:tally, eidolon:false});
	
	const dunamis = await exoDunamis(morphe);
	const eidoTipos = function(x){
		return isMessenger(x)? tipos(x):x;
	}(morphe.eidos);
	const tropos = await getLinkText(eidoTipos,true)
		.then(t=> prop=>prop[arche.prop.tipos]=t);
	if(!topos(dunamis)){
		const stasis = kataStasis(_=>topos(dunamis))(dunamis);
		await app.fileManager.processFrontMatter(dunamis,tropos)
		await stasis.then(_=>dv.index.touch());
	}
	
	return dunamis;
}
```
^gzb1