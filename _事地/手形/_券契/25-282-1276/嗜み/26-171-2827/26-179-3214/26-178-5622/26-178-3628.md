---
型: "[[26-142-0390|ｸｴﾘ]]"
とき: 2026-06-27T21:29:00+09:00
日付: 2026-06-27T21:29:00+09:00
tags:
  - inProgress
通り名: 事地
カタ:
  - "[[DatumOvule.js]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{arche, Mneme: {retrorse, cascade}, Melete: {getLinkText}},
] = await syzygy('26-178-3628');
const {
	path: {cabinet},
	prop: {title, time},
	format: {isStamp},
} = arche;
```
```JS
export default async function (khora){
	khora = dv.array(khora.map(retrorse(cascade)).flat(Infinity))
		.where(p=>isStamp.test(p.file.name))
		.where(p=>p.file.path.startsWith(cabinet+'/')||!p.file.folder)
		.sort(p=>p[title],"asc")
		.sort(p=>moment(p[time]).format(),"asc")
		.array().map(x=>getLinkText(x))
	khora = await Promise.all(khora).then(x=>x.filter(Boolean));
	if(khora.length)this.list(khora);
}
```
^gzb1
