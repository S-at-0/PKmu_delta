---
型: "[[25-201-4985|ﾅｶﾞﾒ]]"
とき: 2026-06-27T20:59:00+09:00
日付: 2026-06-27T20:59:00+09:00
tags:
  - inProgress
通り名: 眺め
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-147-0914|relayTree]]"
  - "[[26-178-3825|公験]]"
  - "[[26-178-3758|言葉]]"
  - "[[26-178-3628|事地]]"
---

- [ ] どちらかというと、自分より下のものを見渡す方が良いか？
***
# あらまし
# 仕掛け
```dataviewjs
dv.view("SrcBarn/khoroScope","26-147-3221")
```
^gzb1

```dataviewjs
dv.view("khoroScope","26-151-5877")
```
### 仕組み方
```JS
const [
	{arche,Mneme:{cascade}},
	{Mneme: {eidolon}, Melete:{isSanctum}},
	relayTree,
] = await syzygy('26-178-5622');
```
```JS
export default async function (noema){
	const khoroEidolon = eidolon(this.current())?.file?.path;
	if(!khoroEidolon)return this.paragraph("❓ 検見先が見つかりません");
	
	/*const noesis = dunamis => isSanctum(true)(dunamis)
		&& eidolon(dunamis)?.file?.path === khoroEidolon;
	if(!noesis(this.current()))return this.paragraph("📭 検見所ではありません");
	
	const kinesis = dunamis => cascade(dunamis).filter(noesis);
	const khora = relayTree(this.current(),kinesis).flat(Infinity);
	*/
	const khora = dv.pages(`"${arche.path.sanctum}"`)
		.where(isSanctum(true))
		.where(x=>eidolon(x)?.file?.path === khoroEidolon)
		.array();
	for(const kytos of cascade(this).slice(-3)){
		this.header(3,kytos[arche.prop.title]);
		await syzygy(kytos).then(f=>f.call(this,khora));
	}
}
```
^gzb1

### 値
```JSON
{
	"isMultiLine": true,
	"isSpaceNeeded": true,
	"block": "```",
	"codeLang": "dataviewjs",
	"content": null,
	"viewPath": "SrcBarn/KhoroScope.js"
}
```
^gzb0