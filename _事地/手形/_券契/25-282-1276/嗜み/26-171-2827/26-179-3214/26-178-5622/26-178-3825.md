---
型: "[[26-142-0390|ｸｴﾘ]]"
とき: 2026-06-27T21:29:00+09:00
日付: 2026-06-27T21:29:00+09:00
tags:
  - inProgress
通り名: 公験
カタ:
  - "[[DatumOvule.js]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{arche, Melete:{getLinkText}},
] = await syzygy('26-178-3825');
```
```JS
export default async function (khora){
	console.log("公験:",khora)
	const noesis = x => dv.pages(`[[${x.file.path}]]`)
		.where(p => p.file.folder === arche.path.mneme)
		.sort(p => p.file.name).array();
	khora = khora.map(noesis).flat(Infinity);

	const tropos = async hyle =>{
	    const kytos = await getLinkText(hyle);
	    return await dv.io.load(hyle?.file?.path)
	        .then(x=>x.split("\n")[0])
	        .then(t=>kytos+": "+t);
	};
	this.list(await Promise.all(khora.map(tropos)));
}
```
^gzb1
