---
型: "[[26-142-0390|ｸｴﾘ]]"
とき: 2026-06-27 17:33
日付: 2026-06-27 17:33
tags:
  - inProgress
通り名: 問い
カタ:
  - "[[DatumOvule.js]]"
aliases:
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{transDunamis, Melete: {getLinkText}},
] = await syzygy('26-178-2573');
```
```JS
export default async function (khora){
	khora = khora.filter(p => p.file.etags.array().includes('#inTerim'))
	console.log("khora:",khora);
	const header = ['事地', 'タグ', '見所'];  
	const tropos = async hyle =>{
		const container = createEl("div");
		await dv.taskList(hyle.file.tasks,false,container,this.component);
		return [await getLinkText(hyle), hyle.file.etags, container];
	}
	this.table(header, await Promise.all(khora.map(tropos)));
}
```
^gzb1
