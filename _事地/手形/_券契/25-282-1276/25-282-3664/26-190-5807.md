---
型: "[[25-201-3900|Modules]]"
とき: 2026-07-09 11:33
日付: 2026-07-09 11:33
tags:
  - inProgress
通り名: 置き場変え
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-158-1704|選び所]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{
		transDunamis,
		arche: {
			format: {linkAlias, isStamp, isMneme},
			path: {logos, mneme, cabinet, sanctum, tally},
		},
		Mneme: {recentView, cascade, retrorse},
		Melete: {flipTo},
	},
	{Mneme: {tipos, eidolon}, Melete: {isSanctum}},
	tropos,
] = await syzygy('26-190-5807');
```
```JS
export default async function (dunamis,kytos){
	dunamis = transDunamis()(dunamis) ?? recentView()?.file;
    if (!dunamis) return new Notice("移す事地が見つかりません");
    
    const morphe = ["/",cabinet,sanctum];
    const [stamp, longMneme, shortMneme] = [
	    isStamp,
	    isMneme.long, 
	    isMneme.short,
	].map(x => x.test(dunamis.basename));
	if(tipos(dunamis))morphe.unshift(tally);
    if(longMneme || shortMneme)morphe.unshift(mneme);
    if( !( stamp || shortMneme || longMneme) )morphe.unshift(logos);
    
    const khoroSkia = cascade(dunamis)
	    .concat(dv.pages(`[[${dunamis.path}]]`).array())
	    .filter(isSanctum(true))
	    .map(x=>x?.file?.folder);
    morphe.unshift(...khoroSkia);
    
    const telos = await tropos([...new Set(morphe.map(transDunamis()))])
    if (telos) return await flipTo(telos.path, dunamis);
    else return new Notice("遣り先がありません");
}
```
^gzb1