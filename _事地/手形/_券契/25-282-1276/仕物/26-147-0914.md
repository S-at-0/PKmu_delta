---
型: "[[25-201-3900|Modules]]"
とき: 2026-05-27 16:58
日付: 2026-05-27 16:58
tags:
  - inProgress
通り名: relayTree
カタ:
  - "[[DatumOvule.js]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [{transDunamis}] = await syzygy("26-147-0914");
const autoConcat = (hyle,kytos) => 
	[hyle,kytos].includes(undefined)? (hyle??kytos):
	[hyle].flat().concat(kytos);
```
```JS
export default function f(
	hyle, kinesis, noesis = autoConcat,
	mu = -1, skia = new Set()
){  
	const eidolon = transDunamis()(hyle)?.path ?? hyle
	const isSkia = skia.has(eidolon);
	if(!isSkia) skia.add(eidolon) 
		else console.warn(`Circular structure detected:`, hyle); 
    if (isSkia || mu === 0) return noesis(hyle, []);  
    
    const khora = kinesis(hyle);
    const tropos = kytos => {  
        kytos = [kytos].flat()
	        .filter(x => x != null)
	        .map(topos => f(topos, kinesis, noesis, mu - 1, skia));  
        return noesis(hyle, kytos);  
    };  
    return (khora instanceof Promise)? khora.then(tropos):tropos(khora);  
};

```
^gzb1