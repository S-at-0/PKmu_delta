---
型: "[[25-201-3900|Modules]]"
とき: 2026-06-07 08:42
日付: 2026-06-07 08:42
tags:
  - inProgress
通り名: 選び所
カタ:
  - "[[DatumOvule.js]]"
  - "[[26-147-1057|パス仕立て]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{arche, transDunamis},
	semaNoesis
] = await syzygy('26-158-1704');
```
```JS
const renderItem = (hyle, el) =>{
	hyle = transDunamis()(hyle);
	const box = el.createDiv();
	const [title, path] = [
		hyle.name || "/",
		hyle.parent?.path ||"直下",
	].map(semaNoesis);
	const content = [
		['div',{text:title}],
		['small', {text:path, attr: {style:"opacity:0.6;"}}],
	]; 
	for(const render of content)box.createEl(...render);
};
export default async function (khora,isMulti=false){
	khora = khora.map(transDunamis()).filter(Boolean);
	const noesis = kytos =>[
		kytos.path,
		transDunamis(true)(kytos)?.[arche.prop.title],
	].join(" ");
	return qa.suggester(noesis,khora,isMulti,'渡す事地をお探し下さい',{renderItem});
	
}
```
^gzb1