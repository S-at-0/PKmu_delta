---
型: "[[25-201-3900|Modules]]"
とき: 2026-05-27 12:42
日付: 2026-05-27 12:42
tags:
  - inProgress
通り名: パス仕立て
カタ:
  - "[[DatumOvule.js]]"
---

***

# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{arche, transDunamis, Mneme: {hyloTipos}}, 
] = await syzygy('26-147-1057');
const {
	format: {isStamp, est, contain, linkAlias},
	prop: {title},
} = arche;
```
```JS
export default function (dunamis){
	const hasStamp = isStamp.source.replaceAll(/^\^|\$$/g, '')
	const noesis = x=>{
		const eidolon = transDunamis(true)(x)?.[title]
		return !eidolon? x: eidolon?.display || eidolon;
	}
	const khora = dunamis
		.replaceAll(RegExp(hasStamp,'g'),noesis)
		.split('/');
	if(!dunamis.includes('/'))return khora.shift();
	
	if(hyloTipos(transDunamis()(dunamis))==='file')
		khora.push(khora.splice(-2,2).join(est));
	return ['Ω', ...khora].filter(Boolean).join(contain);
}
```
^gzb1