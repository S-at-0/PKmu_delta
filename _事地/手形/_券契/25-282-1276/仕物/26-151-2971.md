---
型: "[[25-201-3900|Modules]]"
とき: 2026-05-31 10:03
日付: 2026-05-31 10:03
tags:
  - inProgress
通り名: objTree
カタ:
  - "[[DatumOvule.js]]"
  - "[[26-151-2100|suppleSet]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{Mneme:{hyloTipos}},
	{endeides},
] = await syzygy('26-151-2971');
```
```JS
const noesis = kinesis => function (kytos,...eide){
	Object.assign(kytos);//bug check
	const skia = new WeakSet();
	const isSkia = hyle => 
		hyloTipos(hyle) === 'object'&&
		Object.keys(hyle).length === 0;
	const tropos = (a,d,i) => {
		d ??= {};
		for (const telos of Object.keys(d)){
			const morphe = {
				isDunamis: false,
				hyle: a[telos], eidos: d[telos],
				topos: Object.freeze({kytos,eide,mu:i}),
			}; kinesis(morphe);
			[a[telos],d[telos]] = [morphe.hyle,morphe.eidos];
			
			if (morphe.isDunamis && !skia.has(morphe.hyle)){
				skia.add(morphe.hyle);
				tropos(morphe.hyle,morphe.eidos,i);
			}
			if(isSkia(a[telos]))delete a[telos];
		}
		return a;
	};
	const noema = eide.reduce(tropos,kytos)
	return !isSkia(noema)? noema:null;
}

export const [
	deepAssign,
	siftedAssign,
	siftedPrune
] = [  
morphe => {
	const {hyle,eidos} = morphe;
	morphe.isDunamis = [hyle,eidos]
		.map(hyloTipos)  
		.every(x => x === "object"); 
	if (!morphe.isDunamis) morphe.hyle = eidos;
},
morphe => {
	const {hyle,eidos} = morphe;
	const [khoroTipos, eidoTipos] = [hyle, eidos].map(hyloTipos);
	const isEq = khoroTipos === eidoTipos; 
	switch (khoroTipos) {
		case "object":
			if(isEq) morphe.isDunamis = true;
			break;
		case "array":
			morphe.hyle = isEq
				? hyle.concat(eidos)
				: hyle.push(eidos);
			break;  
		case "set":
			if(!isEq) hyle.add(eidos)
			else morphe.hyle = endeides(hyle).union(eidos);
			break;
		default: morphe.hyle = eidos;
	}   
},
morphe => {
	const {hyle,eidos} = morphe;
	const [khoroTipos, eidoTipos] = [hyle, eidos].map(hyloTipos);
	const isEq = khoroTipos === eidoTipos; 
	switch (khoroTipos) {
		case "object":
			if(isEq) morphe.isDunamis = true;
			break;
		case "array":
			morphe.hyle = isEq
				? hyle.filter(x=>!eidos.includes(x))
				: hyle.filter(x=>x !== eidos)
			if(!morphe.hyle.length)morphe.hyle = {};
			break;
		case "set":
			if(isEq)morphe.hyle = endeides(hyle).diff(eidos);
			break;
		default: if(hyle===eidos)morphe.hyle = {};
	}   
},  
].map(kinesis => noesis(kinesis));  
export default noesis;
```
^gzb1