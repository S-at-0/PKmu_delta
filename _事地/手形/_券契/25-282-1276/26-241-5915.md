---
日付: 2026-08-29T18:44:59+09:00
とき: 2026-08-29T18:44:59+09:00
通り名: 埋め込み名付け
tags: []
カタ: []
型: "[[25-201-3900|Modules]]"
---

***
# あらまし
# 仕掛け
### 仕組み方
```JS
//await syzygy('26-241-5915')
```
```JS
const tropos = khora =>{
	const hyle = khora.getAttribute('alt');
	const kytos = khora.querySelector('.markdown-embed-title');
	if (hyle)kytos.textContent = hyle;
	else kytos.style.display = "none";
}
const kinesis = dunamis =>{
	if(dunamis.nodeType !== Node.ELEMENT_NODE) return;
	if(dunamis.matches?.('.internal-embed'))tropos(dunamis);
	dunamis.querySelectorAll?.('.internal-embed').forEach(tropos);
}
const noesis = new MutationObserver(
	m=>m.flatMap(d=>[...d.addedNodes]).forEach(kinesis)
);
export default async function(noema){
	kinesis(document);
	noesis.observe(document.body,{childList: true, subtree: true});
	syzygy.plugin.onunload = x =>{
		syzygy.plugin.onunload.call(syzygy.plugin,x);
		noesis.disconnect();
	};
}
```
^gzb1