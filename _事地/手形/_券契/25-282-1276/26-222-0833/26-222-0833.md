---
日付: 2026-08-10T05:28:09+09:00
とき: 2026-08-17T20:40:07+09:00
通り名: _繕い
tags:
  - inProgress
  - inTerim
カタ:
  - "[[DossierSilex.js]]"
  - "[[26-178-2793|時付け]]"
型: "[[26-226-4855|CodeEmitter]]"
---

⚠️鋭意制作中。
- [ ] 時の更新。自らよりも古いもののみ実行
- [ ] 実行済みの手形も表示できるように
***
# あらまし
# 仕掛け
### 仕組み方
```JS
const isModule = true;
```
```js
const skia = [
	//無視するファイル名を記す
];
const noesis = async function(){
	const {
		dunamis,
		0: {transDunamis, arche},
		1: endoStamp,
	} = await syzygy('26-222-0833');
	const kytos = transDunamis(true)(dunamis)
	const khora = dv.pages(`"${kytos.file.folder}"`)
		.where(p=>!skia.includes(p.file.name))
		.where(p=>p[arche.prop.time] > kytos[arche.prop.time])
		.where(p=>p.file.path !== kytos.file.path);
	const eidolon = new Set(khora.file.name.array());
	
	if(!eidolon.size)new Notice("繕い事はありません")
	else console.log(["繕い:",...eidolon].join("\n- "));
	
	for(const dunamis of khora)await syzygy(noema)
		.then(f=>f())
		.then(_=>eidolon.delete(dunamis.file.name))
		.catch(console.error);
	await endoStamp(dunamis);
	
	if(eidolon.size){
		const hyle = [
			"⚠️繕いの掛けられない手形がありました。"+
			"コンソールにエラーを記します:",
			...eidolon,
		].join("\n- ");
		new Notice(hyle);
		await sleep(100);
		eidolon.forEach(x=>transDunamis()(x) && endoStamp(x));
	}
	else console.log("all patches have executed!")
}; 
if(typeof isModule ==='undefined')noesis();
```
```JS
export default noesis;
```
^gzb1