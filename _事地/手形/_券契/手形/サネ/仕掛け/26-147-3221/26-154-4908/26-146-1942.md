---
型: "[[25-201-3900|Modules]]"
とき: 2026-05-26 11:19
日付: 2026-05-26 11:19
tags:
  - inProgress
通り名: 型
カタ:
  - "[[26-151-2971|objTree]]"
  - "[[26-151-2100|suppleSet]]"
  - "[[26-154-4908|サネごとの仕込み方]]"
  - "[[26-173-5157|仕立て方]]"
  - "[[25-171-3489|ｶﾀ]]"
---

***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{siftedAssign,siftedPrune},
	{toSet,endeides},
	khoroNoesis,
] = await syzygy('26-146-1942');
```
```JS
export default async function (noema){
	const khora = await khoroNoesis(noema)
		.then(s=>(console.log("型:",s),s))
		.then(s=>Object.groupBy(s,x=>typeof x));
	for(const hyle of new Set(khora.string)){
		const eidos = new Set([noema.dunamis.basename,hyle]);
		noema.eidos = endeides(noema.eidos)
			.union(new Set().add(eidos));
	}
	for(const kytos of new Set(khora.object)){
		const {eidos,skia} = kytos;
		siftedAssign(noema,{eidos,skia});
	}
	noema.eidos &&= endeides(noema.eidos).diff(noema.skia);
};
```
^gzb1