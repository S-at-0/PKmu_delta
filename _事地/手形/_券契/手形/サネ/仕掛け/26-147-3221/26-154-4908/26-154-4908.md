---
型: "[[25-201-3900|Modules]]"
とき: 2026-06-03 13:30
日付: 2026-06-03 13:30
tags:
  - inProgress
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-151-2100|suppleSet]]"
  - "[[25-171-3489|ｶﾀ]]"
  - "[[25-173-3439|ｼｶｹｸﾐ]]"
通り名: サネごとの仕込み方
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{arche, transDunamis, Mneme: {cascade}},
	{getNoesei, Mneme: {topos}},
	{toArray},
] = await syzygy('26-154-4908');
```
```JS
export default async function (noema){
	const [eidoTelos, noetoTelos] = cascade(this)
		.slice(-2)
		.map(x=>x.file.path);
	const noesis = (d,i,s)=> !topos(d)?.telos? false:
		topos(noema.dunamis).telos !== noetoTelos? true:
		topos(d).telos !== noetoTelos? true:
		s.slice(i+1).includes(d);
	const khora = cascade(noema.dunamis).map(x=>x.file.name).filter(noesis)
	if(topos(noema.dunamis).mu > 1)return khora.map(getNoesei(true));
	
	const khoroSkia = (toArray(noema.skia) ?? [])
		.filter(x=>x.shift() === noema.dunamis.basename).flat();
	const tropos = kytos => {
		const {telos,mu} = topos(kytos);
		switch (khoroSkia.includes(kytos)){
			case false: if(telos === noetoTelos)return kytos;
			case true:  if(mu <= 1)return getNoesei()(kytos);
		}
	};
	return Promise.all(khora.map(tropos))
		.then(x=>x.filter(Boolean))
		.then(x=>(console.log("仕込み:",x),x));
}
```
^gzb1