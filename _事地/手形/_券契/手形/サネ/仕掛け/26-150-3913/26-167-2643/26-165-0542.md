---
型: "[[25-201-3900|Modules]]"
とき: 2026-06-14 18:40
日付: 2026-06-14 18:40
tags:
  - inProgress
通り名: 仕掛け綴り
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-151-2100|suppleSet]]"
  - "[[26-136-1442|組み立て]]"
---


***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{transDunamis},
	{getNoesei, Mneme: {messengers}},
	{toArray},
	noesis,
] = await syzygy('26-165-0542');
```
```JS
export default async function (dunamis){
	const khora = await getNoesei()(dunamis)
		.then(x=>toArray(x.eidos).map(d=>d[1]))
		.then(x=>Promise.all(x.map(getNoesei(true))))
		.then(Object.create)
		.catch(console.error);
	if(!khora)new Notice("仕掛け並びが見つかりません");
		
	const noema = await noesis({khora, terminum: 1})
		.then(t=>t.trim())
		.catch(console.error);
	if(!noema)new Notice("写し取れませんでした");
	const isSkia = await app.vault
		.read(transDunamis()(messengers(dunamis).shift()))
		.then(t => t.trim().includes(noema))
		.catch(_=>false);
	if(isSkia)return new Notice("変わりはありませんでした");
	
	return {khora,noema};
}
```
^gzb1