---
型: "[[25-201-4985|ﾅｶﾞﾒ]]"
とき: 2026-05-28 20:50
日付: 2026-05-28 20:50
tags:
  - inProgress
  - inTerim
通り名: 仕掛け並び
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-149-0332|並び組み方]]"
  - "[[26-151-2100|suppleSet]]"
---
***
# あらまし
# 仕掛け
```dataviewjs
dv.view("SrcBarn/khoroScope","26-147-3221")
```
^gzb1

```dataviewjs
dv.view("khoroScope","26-151-5877")
```
### 仕組み方
```JS
const [
	{arche},
	{getNoesei},
	noesis,
	{toArray},
] = await syzygy('26-148-1738');
```
```JS
export default async function (noema){
	const khora = ["eidos","skia"];
	const header = ['仕込み元','','仕掛け',' '];
	const ousioNoema = await getNoesei()(this.current())
		.then(Object)
		.then(toArray)
		.then(Object.create)
		.catch(x=>{console.error(x);return {}})
	for(const [mu,telos] of khora.entries()){
		ousioNoema.topos = {telos};//TODO 値の渡し方
		khora[mu] = await noesis(ousioNoema).catch(console.error);
	}if(!khora.map(x=>x?.[1]?.length).some(Boolean))
		return this.paragraph("📭 組み方が見つかりません");
	
	for (const [telos,eidolon] of khora.filter(x=>x?.[1]?.length)){
		this.header(3,telos);
		this.table(header,eidolon);
	};
}
```
^gzb1

```JSON
{
	"isMultiLine": true,
	"isSpaceNeeded": false,
	"viewPath": "SrcBarn/KhoroScope.js",
	"block": "```",
	"codeLang": "dataviewjs",
	"isMneme": true,
	"isHyle": false
}
```
^gzb0