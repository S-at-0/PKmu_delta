---
型: "[[25-200-5100|ｼｸﾐｶｹ]]"
とき: 2026-05-14 07:27
日付: 2026-05-14 07:27
tags:
  - inProgress
通り名: 受け込み
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-146-1588|仕組み和ぎ選り]]"
  - "[[26-139-5322|掛け形挙げ]]"
  - "[[26-158-1704|選び所]]"
  - "[[26-147-3221|仕立て]]"
  - "[[25-173-3439|ｼｶｹｸﾐ]]"
---

***
# あらまし
# 仕掛け
```dataviewjs
dv.view("SrcBarn/khoroScope","26-147-3221")
```
^gzb1

```dataviewjs
dv.view("khoroScope","26-151-5877")
```
### 仕組み方
```JS
const [
	{arche,transDunamis,Melete:{assignTo,getLinkText}},
	{Mneme:{topos}},
	kinetoTropos,
	khoroNoesis,
	tropos,
	kinesis,
] = await syzygy('26-134-0681');
```
```JS
const noesis = async function (noema){
	const ousia = transDunamis()(this.current())
	const noesis = await function (x){
		const noesis = hyle => 
			hyle[arche.prop.title]
			??　hyle.file.name;
		switch(x.length){
			case 0 : return null;
			case 1 : return x.shift();
			default: return qa.suggester(noesis,x);
		}
	}(kinetoTropos(this,topos(ousia).telos));
	if(!noesis)return new Notice("受け込み方がありません");
	
	const hyle = await khoroNoesis
		.call(this,noema,noesis.file.path)
		.then(tropos).then(x=>getLinkText(x,true))
		.catch(console.error);
	if(!hyle) return new Notice("受け込み元がありません");
	 
	const noetoTelos = cascade(this).pop().file.path;
	const morphe = topos(this.current())?.telos === noetoTelos
		? Array(2).fill(hyle): hyle;
	await assignTo(ousia,arche.prop.cascade,morphe);
	new Notice("仕込まれた為、値を仕立て直します。");
	await kinesis.call(this,ousia);
}

export default async function (noema){
	return noesis.bind(this,noema);
}
```
^gzb3
### 値
```JSON
{
	"viewPath": "SrcBarn/khoroScope.js",
	"isMneme": true,
	"isHyle": true,
	"text": "受け込み",
	"isMultiLine": true,
	"codeLang": "dataviewjs",
	"block": "```",
	"isSyzygy": true,
	"eidolon": {
		"26-192-2754": 1,
		"26-229-5867": 1
	}
}
```
^gzb0