---
型: "[[26-142-0390|ｸｴﾘ]]"
とき: 2026-05-22 17:55
日付: 2026-05-22 17:55
tags:
  - inProgress
通り名: 仕組み掛け方
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
---


***
# あらまし
[[25-200-5100|ｼｸﾐｶｹ]]の現しは `isHyle: true` なので、擬似的にnoema.hyleをthisへ映し、現しを動かす。
現しはバックグラウンド処理を可能にするため、関数を返すコールバックとする。

# 仕掛け
### 仕組み方
```JS
const [
	{arche,transDunamis},
	{getNoesei, Mneme:{tipos}},
] = await syzygy('26-142-5513');
```
```JS
export default async function (noema){
	const {
		text, 
		style='display: block; margin: auto;',
		format,
	} = Object(noema?.hexis);
	const onclick = await syzygy(noema.dunamis)
		.then(f=>f.call(this,noema))
	    .catch(e =>{console.error(e);new Notice(e.message)});
	const button = this.el("button",text);  
	Object.assign(button,{onclick,style});
	
	for(const x of [format].flat().filter(Boolean))
		Object.assign(button.style,Object(arche.format[x]));
}
```
^gzb1
