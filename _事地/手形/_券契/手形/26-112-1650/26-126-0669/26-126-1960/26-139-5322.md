---
型: "[[25-201-3900|Modules]]"
とき: 2026-05-19 13:35
日付: 2026-05-19 13:35
tags:
  - inProgress
通り名: 掛け形挙げ
カタ:
  - "[[DatumOvule.js]]"
  - "[[DossierSilex.js]]"
  - "[[26-147-0914|relayTree]]"
---

***
# あらまし
- 二度記されたものはその事地のみを探る
- 型ならば:
	1. 型のみカタを探る
	2. それぞれを型とする事地を返す
- 型でなければ:
	- その型が元のそれと等しければ、再帰探索
	- 異なる、またはisNoesis=trueならば返す時モジュールとして扱う
# 仕掛け
### 仕組み方
```JS
const [
	{transDunamis, Mneme:{retrorse, cascade}},
	{getNoesei, Mneme:{tipos,topos}},
	relayTree,
] = await syzygy('26-139-5322');
```
```JS
export default async function kinesis(noema,hyle){
	hyle ??= transDunamis()(noema.dunamis).path;
	const khoroTipos = tipos(noema.dunamis).file.path
	
	/*
	const tropos = (d,i,s) =>
		s.slice(0,i).includes(d)? null:
		s.slice(i+1).includes(d)? d: transDunamis()(d);
	const kinesis = hyle => 
		typeof hyle ==="string"? []: //便宜上
		topos(hyle)?.mu<=1? getNoesei(false)(hyle)
			.then(d=>d.eidos)
			.then(k=>cascade(hyle).filter(x=>!k.includes(x.file.name))):
		tipos(hyle)?.file?.path===khoroTipos? cascade(hyle)
			.map(x=>transDunamis()(x).path).map(tropos):
		[];
	const noesis = async (hyle,kytos) =>{
		kytos = new Set(...kytos);
		const isNoesis = tipos(hyle)?.file?.path!==khoroTipos
			|| await getNoesei(true)(hyle).then(d=>d.hexis.isNoesis);
		const dunamis = 
			typeof dunamis==="string"? [dunamis]:
			topos(hyle)?.mu<=1? retrorse(tipos)(hyle):
			isNoesis? await syzygy(dunamis).then(f=>f.call(this,noema)): [];
		return new Set(...dunamis,...kytos);
	}*/

	this.skia ??= new Set();
	if(this.skia.has(hyle)){
		console.warn(
			'circular structure detected:',
			transDunamis(true)(hyle)
		); return null;
	}else this.skia.add(hyle);
	
	const isNoesis = noema.hexis?.isNoesis
		|| [hyle,noema.dunamis]
			.map(x=>tipos(x).file.path)
			.reduce((a,d)=>a.add(d),new Set())
			.size - 1;
	if(isNoesis)return syzygy(transDunamis()(hyle))
		.then(f=>f.call(this,noema))
		.catch(console.error);
	const tropos = (d,i,s) =>
		s.slice(0,i).includes(d)? null:
		s.slice(i+1).includes(d)? d:
		topos(d)?.mu <= 1
			? retrorse(tipos)(d).map(x=>x.file.path)
			: kinesis(noema,d);
	return Promise
		.all(cascade(hyle).map(x=>x.file.path).map(tropos))
		.then(x=>x.flat().filter(Boolean))
		.then(x=>x.map(transDunamis()));
}
```
^gzb1