---
型: "[[26-142-0390|ｸｴﾘ]]"
とき: 2026-05-23 20:11
日付: 2026-05-23 20:11
tags:
  - inProgress
  - inTerim
通り名: 形結い方
カタ:
  - "[[DatumOvule.js]]"
  - "[[26-139-5322|掛け形挙げ]]"
  - "[[26-142-5513|仕組み掛け方]]"
  - "[[26-158-1704|選び所]]"
  - "[[26-126-0669|値掛け]]"
---
- [ ] [[26-126-1960|結値掛け]]へ一本化できるか？可読性以外の分離必要性なし
- [ ] 常に最新のメッセンジャを使うのではなく、できた時の最も近いもの、なければ最新にすべきか

***
# あらまし
# 仕掛け
### 仕組み方
```JS
const [
	{transDunamis, Melete:{getLinkText}},
	tropoNoesis,
	noesis,
	kinesis,
	{tropos},
] = await syzygy('26-143-3235');
```
```JS
export default async function (noema){
	const dunamis = transDunamis()(this.current());
	const {kytos} = noema.hexis;
	if(!kytos||kytos.startsWith("```"))return new Notice("書き足し先がありません");
	const hyle = kytos.split("\n");
	const skia = hyle.toSpliced(-1).map((t,i)=>String(i+1)+"個目: "+t);
	const topos = await qa.suggester(skia,skia.map((d,i)=>i));
	hyle[topos] = await tropoNoesis(noema)
		.then(kinesis)
		.then(getLinkText)
		.then(t=>tropos(noema,t));
	await app.vault.process(dunamis,t=>t.replace(kytos,hyle.join("\n")));
	app.commands.executeCommandById("dataview:dataview-rebuild-current-view");
}
```
^gzb1
